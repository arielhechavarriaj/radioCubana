// scraper/src/sources/portal.ts
import { Radio } from '../types/radio.js';

export async function scrapePortalUrls(radios: Partial<Radio>[]): Promise<Partial<Radio>[]> {
  // Podríamos intentar obtener URLs desde el portal oficial
  return radios;
}